# Let's Encrypt ACME challenge location
# Include this in server blocks that need to handle certificate validation

location /.well-known/acme-challenge/ {
    root /var/www/certbot;
    allow all;
}
